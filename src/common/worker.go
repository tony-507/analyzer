package common

type worker struct {
	graph []interface{}
}

// func (wk *worker) runGraph() {
// 	emptyUnit :=
// 	for _, plugin := range wk.graph {
// 		reflect.ValueOf(&plugin).MethodByName("")
// 	}
// }
